{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "sldr config schema",
  "description": "Configuration schema for sldr (main config.toml)",
  "type": "object",
  "properties": {
    "config": {
      "$ref": "#/$defs/CoreConfig",
      "default": {
        "agent": "opencode",
        "default_flavor": "default",
        "dev_port": "3030",
        "flavor_dir": "~/.config/sldr/flavors",
        "layout_dir": "~/.config/sldr/layouts",
        "library": "~/sldr",
        "scaffold_dir": "~/.config/sldr/scaffolds"
      }
    },
    "matching": {
      "$ref": "#/$defs/MatchingConfig",
      "default": {
        "max_suggestions": 6,
        "resolution_order": [
          "anchor",
          "exact",
          "fuzzy",
          "index",
          "interactive"
        ],
        "threshold": 50.0
      }
    },
    "presentations": {
      "$ref": "#/$defs/PresentationsConfig",
      "default": {
        "output_dir": "~/sldr/presentations",
        "playlist_dir": "~/sldr/playlists",
        "slide_dir": "~/sldr/slides"
      }
    }
  },
  "$defs": {
    "CoreConfig": {
      "description": "Core application settings",
      "type": "object",
      "properties": {
        "agent": {
          "description": "Preferred AI agent for slide generation",
          "type": "string",
          "default": "opencode"
        },
        "default_flavor": {
          "description": "Default flavor to use when none specified",
          "type": "string",
          "default": "default"
        },
        "dev_port": {
          "description": "Port for the local dev/watch server",
          "type": "string",
          "default": "3030"
        },
        "flavor_dir": {
          "description": "Directory containing flavors (themes/styles)",
          "type": "string",
          "default": "~/.config/sldr/flavors"
        },
        "layout_dir": {
          "description": "Directory containing user layouts (override/extend built-ins by name)",
          "type": "string",
          "default": "~/.config/sldr/layouts"
        },
        "library": {
          "description": "Library root: the single self-sufficient tree carrying slides,\nlayouts, flavors, playlists, scaffolds, and media.\nAsset resolution searches the library first, then the extra dirs\nbelow, then the built-ins embedded in the binary.",
          "type": "string",
          "default": "~/sldr"
        },
        "scaffold_dir": {
          "description": "Extra directory containing slide scaffolds (searched after the\nlibrary's scaffolds/)",
          "type": "string",
          "default": "~/.config/sldr/scaffolds"
        }
      }
    },
    "MatchingConfig": {
      "description": "Fuzzy matching configuration",
      "type": "object",
      "properties": {
        "max_suggestions": {
          "description": "Maximum number of suggestions to show",
          "type": "integer",
          "format": "uint",
          "default": 6,
          "minimum": 0
        },
        "resolution_order": {
          "description": "Order in which to try resolution methods",
          "type": "array",
          "default": [
            "anchor",
            "exact",
            "fuzzy",
            "index",
            "interactive"
          ],
          "items": {
            "type": "string"
          }
        },
        "threshold": {
          "description": "Minimum fuzzy match score (0-100)",
          "type": "number",
          "format": "double",
          "default": 50.0
        }
      }
    },
    "PresentationsConfig": {
      "description": "Presentations and slides configuration",
      "type": "object",
      "properties": {
        "output_dir": {
          "description": "Directory for generated presentations",
          "type": "string",
          "default": "~/sldr/presentations"
        },
        "playlist_dir": {
          "description": "Directory containing presentation playlists",
          "type": "string",
          "default": "~/sldr/playlists"
        },
        "slide_dir": {
          "description": "Directory containing individual slide files",
          "type": "string",
          "default": "~/sldr/slides"
        }
      }
    }
  }
}