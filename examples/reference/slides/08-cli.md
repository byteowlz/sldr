---
title: The CLI
description: Terminal layout — code framed as a terminal window
tags: [reference, guide]
layout: terminal
---

# Five commands you need

```bash
sldr init                        # set up the library
sldr new intro --scaffold title  # create a slide
sldr playlist create             # define a deck (JSON via stdin)
sldr build my-talk --flavor fjord
sldr open my-talk
```
