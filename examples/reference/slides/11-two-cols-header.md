---
title: Header Band
description: Two-cols-header layout variant
tags: [reference, guide]
layout: two-cols-header
---

# Same markers, different frame

::left::
`two-cols-header` reads the exact same `::left::` / `::right::` markers.

::right::
Structure is the layout's job. Your content never changes when the structure does.
